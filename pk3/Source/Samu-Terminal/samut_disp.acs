/**
*
* 	<samut_disp.acs> 
* 	Display module!
*
**/

function void SamuTerm_Display(void){

	int i = 0, rect;
	ShowBackground();
	
	for (i = 0; i < comp_count[STCOMP_IBTN]; i++){
		if(getImgButtonShow(i)){
		rect = getIMGButtonRect(i);
			if (RectIsHovered(rect) && comphover){
				//localAmbientSound("SamuTerm/Hover",127);
				HilightImgButton(i);
			}
		ShowImgButton(i);
		}
	}
	
	for (i = 0; i < comp_count[STCOMP_LBL]; i++){
			if(Label_getShow(i))
			ShowLabel(i);
	}
	
	for (i = 0; i < comp_count[STCOMP_IMG]; i++){
			if(getImageShow(i))
			ShowImage(i);
	}
	
	for (i = 0; i < comp_count[STCOMP_SBTN]; i++){
		if(getStrButtonShow(i)){
		rect = getStrButtonRect(i);
			if(RectIsHovered(rect) && comphover){
				//localAmbientSound("SamuTerm/Hover",127);
				HilightStrButton(i);
			}
			ShowStrButton(i);
		}
	}
	
	/*
	for(i = 0; i < pancount; i++){
		ShowPanels(i);
	}*/
	
	if(getPromptState())ShowPrompt();
	
	ShowCursor();
	
}

#include "samut_disp_funs.acs"
#include "samut_disp_comps.acs"