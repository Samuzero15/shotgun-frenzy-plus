
//So a "page" is structured in basically 2 functions.

//This will update the page with data (counting stuff, n items left etc.)
function void UpdateTemplatePage(int pageTag){
	setPageTitle(pageTag, "Title", CR_CYAN);
	if(isOnTheSamePage(pageTag)){
		
	}
}

//This will initialize the page components.
function void InitTemplatePage(int pageTag){

}